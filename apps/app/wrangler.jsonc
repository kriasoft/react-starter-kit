{
  "$schema": "../../node_modules/wrangler/config-schema.json",

  // [METADATA]
  // App worker accessed via service binding from web worker (no routes needed).
  "name": "example-app",
  "compatibility_date": "2026-08-08",
  "workers_dev": false,
  "observability": { "enabled": true },

  // [ASSETS]
  // Serves bundled JavaScript, CSS, images, and other static assets.
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  },

  // [ENV:PRODUCTION]
  // Command: bun wrangler deploy --env=""
  "vars": {
    "ENVIRONMENT": "production"
  },

  "env": {
    // [ENV:DEVELOPMENT]
    // Named Wrangler development environment; `bun dev` uses Vite instead.
    "dev": {
      "vars": {
        "ENVIRONMENT": "development"
      }
    },

    // [ENV:STAGING]
    // Command: bun wrangler deploy --env staging
    "staging": {
      "vars": {
        "ENVIRONMENT": "staging"
      }
    }
  }
}
