import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Infrastructure Terraform Specification","description":"","frontmatter":{},"headers":[],"relativePath":"specs/infra-terraform.md","filePath":"specs/infra-terraform.md","lastUpdated":1786911472000}`),a={name:`specs/infra-terraform.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<div style="display:none;" hidden data-nosnippet>Are you an LLM? You can read better optimized documentation at /specs/infra-terraform.md for this page in Markdown format</div><h1 id="infrastructure-terraform-specification" tabindex="-1">Infrastructure Terraform Specification <a class="header-anchor" href="#infrastructure-terraform-specification" aria-label="Permalink to “Infrastructure Terraform Specification”">​</a></h1><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to “Scope”">​</a></h2><p>Terraform provisions the durable Cloudflare resources the workers consume. It does not manage workers. See <a href="/adr/002-terraform-wrangler-boundary">ADR-002</a> for the decision.</p><table tabindex="0"><thead><tr><th>Owned by Terraform</th><th>Owned by Wrangler</th></tr></thead><tbody><tr><td><code>cloudflare_hyperdrive_config</code> × 2/env</td><td>Worker names, code, versions, deployments</td></tr><tr><td><code>cloudflare_r2_bucket</code> + <code>_cors</code> (opt-in)</td><td>Routes, custom domains, service bindings, vars, secrets, assets</td></tr></tbody></table><p>The uploads bucket carries its own CORS policy: browsers reject a presigned <code>PUT</code> to a bucket without one, so the policy belongs with the Terraform-owned bucket rather than as a dashboard step. It allows <code>PUT</code> with <code>Content-Type</code> from <code>uploads_cors_origins</code>, which must be explicit – a <code>*</code> origin is rejected at plan time.</p><p><strong>Invariant:</strong> no field is configured by both tools. The only values crossing the boundary are stable, non-secret resource identifiers – the two Hyperdrive IDs, plus the R2 bucket name when uploads are enabled.</p><p><strong>Non-goals:</strong> worker deployment, DNS, multi-region orchestration, blue-green deploys, autoscaling.</p><h2 id="layout" tabindex="-1">Layout <a class="header-anchor" href="#layout" aria-label="Permalink to “Layout”">​</a></h2><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">infra/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  modules/cloudflare/</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # Resources, connection parsing, naming</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  envs/</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">{staging,production}</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">/</span></span></code></pre></div><p>One root per environment, one HCP Terraform workspace per root. Each root hard-codes its own <code>environment</code> and workspace name, so it cannot create another environment&#39;s resources or write its state.</p><p>There is no <code>dev</code> root. Local development uses <code>CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_&lt;BINDING&gt;</code> and provisions nothing.</p><p>There is no <code>preview</code> root either. A single shared preview environment is not per-PR isolation – concurrent pull requests would overwrite each other&#39;s deployment and share one database – and it duplicates staging&#39;s role. Add an environment by copying a root and adding a matching <code>wrangler.jsonc</code> block.</p><h2 id="versions" tabindex="-1">Versions <a class="header-anchor" href="#versions" aria-label="Permalink to “Versions”">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Constraint</th></tr></thead><tbody><tr><td>terraform</td><td><code>&gt;= 1.12, &lt; 2</code></td></tr><tr><td>cloudflare</td><td><code>~&gt; 5.0</code></td></tr></tbody></table><p>Version constraints live in env roots; the module declares <code>source</code> only. Lock files are committed and cover linux, macOS and Windows.</p><h2 id="hyperdrive" tabindex="-1">Hyperdrive <a class="header-anchor" href="#hyperdrive" aria-label="Permalink to “Hyperdrive”">​</a></h2><p>Two configurations per environment, matching the two bindings in <code>apps/api/</code>:</p><table tabindex="0"><thead><tr><th>Name</th><th><code>caching.disabled</code></th><th>Binding</th></tr></thead><tbody><tr><td><code>&lt;slug&gt;-&lt;env&gt;-cached</code></td><td><code>false</code></td><td><code>HYPERDRIVE_CACHED</code></td></tr><tr><td><code>&lt;slug&gt;-&lt;env&gt;-uncached</code></td><td><code>true</code></td><td><code>HYPERDRIVE_UNCACHED</code></td></tr></tbody></table><p><code>cache_max_age_seconds</code> (60) and <code>cache_stale_while_revalidate_seconds</code> (15) are set explicitly rather than inherited, so the staleness window is reviewable in the configuration.</p><p><code>origin_connection_limit</code> is a soft maximum per configuration – Cloudflare may briefly exceed it. It defaults to 20, inside Cloudflare&#39;s Workers Free ceiling so the starter works on any plan. Both env roots expose it so it can be raised per deployment; the origin sees twice the value.</p><h3 id="connection-url" tabindex="-1">Connection URL <a class="header-anchor" href="#connection-url" aria-label="Permalink to “Connection URL”">​</a></h3><p><code>database_url</code> is split by one anchored regex into the discrete fields Hyperdrive stores. Accepted shape:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>postgres[ql]://USER:PASSWORD@HOST[:PORT]/DATABASE[?params]</span></span></code></pre></div><p>The port defaults to <code>5432</code>; query parameters are ignored, since TLS to the origin is Hyperdrive&#39;s setting rather than the client&#39;s.</p><p>Three failures are rejected at plan time rather than surfacing later as connection errors:</p><table tabindex="0"><thead><tr><th>Rejected</th><th>Why</th></tr></thead><tbody><tr><td>Malformed URL</td><td>Nothing to parse.</td></tr><tr><td>Percent-encoded credentials</td><td>Terraform has no <code>urldecode</code>, so the escape sequence would be stored verbatim.</td></tr><tr><td>A <code>-pooler</code> host</td><td>Hyperdrive is the pool; Neon&#39;s transaction-mode pooler breaks its prepared statements.</td></tr></tbody></table><h2 id="state" tabindex="-1">State <a class="header-anchor" href="#state" aria-label="Permalink to “State”">​</a></h2><p>HCP Terraform, via a <code>cloud</code> block – state, locking and version history in one place, with no bucket to provision first. See <a href="/adr/003-hcp-terraform-state">ADR-003</a>.</p><p>Each root pins its own <code>cloud</code> block – <code>hostname</code>, organization and <code>workspaces { name = &quot;example-&lt;env&gt;&quot; }</code>. Adopters replace the organization and workspace placeholders during setup. One directory therefore selects both the resource names and the state.</p><p>Pinning the name rather than supplying <code>TF_WORKSPACE</code> is what makes the binding structural. Terraform refuses to run when <code>TF_WORKSPACE</code> disagrees with <code>workspaces.name</code>, and falls back to <code>TF_CLOUD_HOSTNAME</code> only when <code>hostname</code> is omitted, so no ambient shell state can point a root at another environment or host. That also covers <code>output</code>, <code>import</code> and the <code>state</code> subcommands, which never evaluate output preconditions and so could not be guarded by one.</p><p>Terraform is invoked through two package scripts, <code>infra:staging</code> and <code>infra:production</code>, which are shorthand for <code>terraform -chdir=infra/envs/&lt;environment&gt;</code>.</p><p>Each workspace must also set <strong>Terraform Working Directory</strong> to <code>envs/&lt;env&gt;</code>. Remote runs upload the configuration, and a root referencing <code>../../modules/cloudflare</code> reaches outside its own directory; Terraform uploads parent directories only when a working directory is set, to the depth of that setting. There is no <code>cloud</code> block argument for it. It is one of five settings the repository cannot declare, alongside the CLI-driven workflow, Remote execution mode, the workspace Terraform version and the variables themselves – see <a href="/adr/003-hcp-terraform-state">ADR-003</a> and <code>infra/README.md</code>.</p><p><strong>State is credential-bearing.</strong> Hyperdrive stores the origin password as a discrete field, so it is written to state. <code>sensitive = true</code> hides values from output; it does not encrypt them. HCP Terraform encrypts state at rest and retains every version.</p><h2 id="credentials" tabindex="-1">Credentials <a class="header-anchor" href="#credentials" aria-label="Permalink to “Credentials”">​</a></h2><p>Runs execute in HCP Terraform, which supplies these from the workspace. The two kinds behave differently under a CLI-driven run: a Terraform variable is a default that a local <code>TF_VAR_*</code> overrides for that run, while no other environment variable reaches the run, so the provider token can only come from the workspace.</p><table tabindex="0"><thead><tr><th>Kind</th><th>Supplied as</th></tr></thead><tbody><tr><td>Cloudflare provider</td><td><code>CLOUDFLARE_API_TOKEN</code> workspace environment var</td></tr><tr><td>Origin database</td><td><code>database_url</code> workspace variable, sensitive</td></tr><tr><td>Application secrets</td><td><code>wrangler secret put</code> – never Terraform</td></tr></tbody></table><p>The Cloudflare token needs <strong>Account → Hyperdrive → Edit</strong>, plus <strong>Account → Workers R2 Storage → Edit</strong> only when the uploads bucket is enabled. It needs no zone or worker scopes.</p><p>The infrastructure workflow holds one credential: <code>TF_API_TOKEN</code>, an HCP Terraform team token (a group token on HCP Terraform Europe). It is an environment secret on all four infrastructure environments, not a repository secret: the deployment-branch rule only binds jobs that name the environment, so a repository-scoped token would let a branch drop <code>environment:</code> and keep the credential.</p><p>One name, three tokens: a shared Plan token for both <code>-plan</code> environments, and one Write token per workspace for <code>infra-staging-apply</code> and <code>infra-production-apply</code>. Scoping the apply tokens per workspace is what makes the production gate real – a single apply token would need Write on both, letting the ungated staging environment apply production. The plan token is shared because planning either workspace bypasses no gate. Team management requires HCP Essentials or above; on Free, leave the token unset and run Terraform from a trusted machine.</p><p>Application deploys use <code>staging</code> and <code>production</code>, so a Cloudflare token and an HCP token are never reachable from the same job.</p><p>Terraform&#39;s Cloudflare and database credentials are never stored in GitHub. The separate worker-deploy workflow does hold a <code>CLOUDFLARE_API_TOKEN</code> – Cloudflare&#39;s <strong>Edit Cloudflare Workers</strong> template, which grants the worker, route and DNS scopes Terraform deliberately lacks.</p><p>Setting a workspace&#39;s execution mode to <strong>Local</strong> stores state remotely but runs Terraform on the caller&#39;s machine; HCP then ignores workspace variables, so values come from <code>TF_VAR_*</code> or a gitignored <code>terraform.tfvars</code>.</p><h2 id="naming" tabindex="-1">Naming <a class="header-anchor" href="#naming" aria-label="Permalink to “Naming”">​</a></h2><p>Resource values use <code>{project_slug}-{environment}[-role]</code>, lowercase <code>^[a-z0-9-]+$</code>. <code>project_slug</code> must match the worker name prefix in <code>apps/*/wrangler.jsonc</code>.</p><p>Resource identifiers name the concrete thing (<code>cloudflare_hyperdrive_config.cached</code>); module names describe the architectural role (<code>module.edge</code>).</p><h2 id="ci" tabindex="-1">CI <a class="header-anchor" href="#ci" aria-label="Permalink to “CI”">​</a></h2><p><code>.github/workflows/ci.yml</code> runs <code>bun infra:check</code> – <code>terraform fmt -check</code>, then <code>init -backend=false -lockfile=readonly</code> and <code>validate</code> for each root – on pull requests to <code>main</code>, on <code>main</code> itself, and on manual CI runs. <code>init -backend=false</code> installs providers and modules without accessing the configured HCP backend, which is the sequence HashiCorp documents for validation, so CI needs no HCP or Cloudflare credentials. <code>-lockfile=readonly</code> makes every automated <code>init</code>, here and in <code>infra.yml</code>, consume the committed provider selections rather than amend them on a runner: a provider change has to arrive as a reviewed lock-file diff.</p><p>Every workflow installs Terraform with <code>terraform_version: &quot;&gt;=1.12.0 &lt;2.0.0&quot;</code> – the roots&#39; <code>&gt;= 1.12, &lt; 2</code> in the SemVer syntax the action uses, and the same range the HCP workspaces express as <code>~&gt; 1.12</code>. HCP and <code>setup-terraform</code> each select a binary within it and may differ for a day after a release, while each root rejects anything outside it. No exact release is pinned, so a Terraform release needs no action; raising the floor means editing all four declarations.</p><p><code>.github/workflows/infra.yml</code> plans and applies, on manual dispatch only, serialised per environment. One dispatch runs a plan job that saves the plan and publishes it to the run summary, then an apply job that applies that saved plan rather than recomputing one – gated by required reviewers where the repository&#39;s GitHub subscription supports them. The plan job uses a reviewer-free <code>-plan</code> environment because protection rules gate any job naming an environment, so a shared one would place the approval before the plan existed. The apply job is skipped when the plan reports no changes.</p><p>Both jobs are restricted to <code>main</code> – by a first-step check in the workflow, and by a deployment-branch rule on each GitHub Environment, which is the half a branch cannot edit away. A speculative plan is not a read-only preview: HCP executes the configuration it is handed in the workspace&#39;s privileged run environment, with its variables and state, which is why HashiCorp treats plan permission as equivalent to write. Branch-level feedback comes from <code>bun infra:check</code> in <code>ci.yml</code>, which needs no credentials. Application deploys never run Terraform.</p>`,51)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};