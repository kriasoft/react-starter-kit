name: Deploy

on:
  workflow_call:
    inputs:
      name:
        description: "Name of the deployment"
        required: true
        type: string
      environment:
        required: true
        type: string
      url:
        description: "URL of the deployment"
        required: true
        type: string

jobs:
  deploy:
    name: ${{ inputs.name }}
    runs-on: ubuntu-latest
    permissions:
      deployments: write
      pull-requests: read
    environment:
      name: ${{ inputs.environment }}
      url: ${{ steps.pr.outputs.formatted || inputs.url }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v6
        with:
          name: build
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - uses: kriasoft/pr-codename@v1
        id: pr
        if: contains(inputs.url, '{codename}')
        with:
          template: ${{ inputs.url }}
          token: ${{ github.token }}
      # TODO: Enable after provisioning Cloudflare and configuring the deploy
      # credentials in each GitHub environment.
      #
      # Production uses the top-level Wrangler config; staging and preview are
      # named environments. Wrangler rejects `--env production`.
      # - name: Deploy workers
      #   env:
      #     CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     DEPLOY_ENV: ${{ inputs.environment }}
      #   run: |
      #     if [[ "$DEPLOY_ENV" == "production" ]]; then
      #       env_args=()
      #     else
      #       env_args=(--env "$DEPLOY_ENV")
      #     fi
      #     bun wrangler deploy --config apps/api/wrangler.jsonc "${env_args[@]}"
      #     bun wrangler deploy --config apps/app/wrangler.jsonc "${env_args[@]}"
      #     bun wrangler deploy --config apps/web/wrangler.jsonc "${env_args[@]}"
